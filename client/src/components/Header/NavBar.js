import React from "react";
import { Link } from "react-router-dom";

const NavBar = () => {
  return (
    <div>
      <button>Products</button>
      <h1>Unknown store name</h1>
      <button>Cart</button>
    </div>
  );
};

export default NavBar;
